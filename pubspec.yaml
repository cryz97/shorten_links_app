name: shorten_links_app
description: nubank mobile test.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito:
  build_runner:

flutter:
  uses-material-design: true

